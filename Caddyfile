{
	email off
}

loom.grogan.dev {
	reverse_proxy loomd:8080
}

# App hostnames are generated by loomd (`caddy.rs`) once a service is
# deployed. Until then, only the control plane is routed here. Do not
# send `*.apps.grogan.dev` to loomd — that would hide missing apps.
